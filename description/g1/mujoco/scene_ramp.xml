<mujoco model="g1_scene_ramp">
  <!--
    Flat ground with a gentle 3 degree ramp starting flush at the origin and
    rising along +x. For testing terrain generalisation: the stack has to notice
    that the ground is no longer at z = 0 and place the swing foot accordingly.

    slope.xml is not usable for this. It sits at x in [-5, -1] and, being a
    rotated box with its low corner in the air, presents a ~0.27 m ledge at its
    near edge - a step up, not a ramp. Here the box is deliberately thick (0.5 m
    half-thickness) and sunk so that the LOW edge of its top surface lands exactly
    on (x, z) = (0, 0), which makes the transition from floor to ramp seamless.

    Geometry: half-size (2, 1, 0.5), rotated -3 deg about Y so the surface rises
    with +x (a positive rotation about +Y tilts local +x downward, hence the
    negative sign), centred at (2.023, 0, -0.3946). That puts the top surface at
    z = 0 at x = 0 and z = 0.209 at x = 3.99.
  -->

  <!-- legged robot -->
  <include file="g1_29dof.xml"/>

  <!-- Match the legacy whole-body MuJoCo simulation timestep. -->
  <option timestep="0.0005"/>

  <statistic center="1.0 0.7 1.0" extent="0.8"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.1 0.1 0.1" specular="0.9 0.9 0.9"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="-140" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb2="0.835 0.761 0.612" rgb1="0.639 0.722 0.8" width="512" height="512"/>

    <texture type="2d" name="groundplane" builtin="checker" mark="random"
         rgb1="0.25 0.3 0.35" rgb2="0.15 0.2 0.25"
         markrgb="0.6 0.7 0.8"
         width="512" height="512"/>

    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="2 2" reflectance="0.05"/>
  </asset>

  <worldbody>
    <light pos="1 0 3.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"/>

    <body name="ramp" pos="2.023 0 -0.394643">
      <geom name="ramp_surface"
            type="box"
            size="2.0 1.0 0.5"
            quat="0.9996573 0 -0.0261769 0"
            rgba="0.55 0.6 0.65 1"
            friction="0.8 0.1 0.001"/>
    </body>
  </worldbody>

  <contact>
    <!--
      The ramp is a thick box sunk below the surface it presents, so most of its
      volume is under the infinite floor plane. Without this exclusion MuJoCo
      resolves that as an enormous penetration between the two static geoms and
      launches the scene on the first step - the robot was falling at t = 0.3 s,
      before it had taken a step. Both still collide with the robot normally;
      only the floor/ramp pair is skipped.
    -->
    <exclude body1="world" body2="ramp"/>
  </contact>

</mujoco>
